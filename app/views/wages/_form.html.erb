<%= form_for(wage) do |f| %>
  <% if wage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wage.errors.count, "error") %> prohibited this wage from being saved:</h2>

      <ul>
      <% wage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :time_logged %>
    <%= f.time_select :time_logged %>
  </div>

  <div class="field">
    <%= f.label :hourly_rate %>
    <%= f.number_field :hourly_rate %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
